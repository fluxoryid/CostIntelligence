{
  "name": "costintelligence",
  "version": "2.1.0",
  "private": true,
  "description": "HPS Intelligence Production 2.1 deployment for Cloudflare Workers",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "npm test",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
