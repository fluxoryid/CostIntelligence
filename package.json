{
  "name": "costintelligence",
  "version": "2.0.0-rc.1",
  "private": true,
  "description": "HPS Intelligence production deployment for Cloudflare Workers",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "npm test",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
